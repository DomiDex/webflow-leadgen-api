    # Environment variables
        .env

        # Deno lock file (optional but good practice if you use one)
        # deno.lock

        # Other common ignores
        node_modules

        # Node/Deno modules (if cached locally, though Deno manages globally)
        deno-cache/

        # IDE specific
        .vscode/
        .idea/

        # OS specific
        .DS_Store
        Thumbs.db